export const totalWidth = 10000; //center of monitor
export const totallHeight = 8000;
export const devicePixelRatio = window.devicePixelRatio || 1;
export const width = innerWidth;
export const height = innerHeight;


export const scale = totallHeight / height ; //scale must have a float type
